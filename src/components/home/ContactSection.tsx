"use client";

import React from "react";
import { Column, Text } from "@once-ui-system/core";

export default function ContactSection() {
  return (
    <Column fillWidth fillHeight center style={{ backgroundColor: 'var(--surface)', flex: 1, minHeight: 0 }}>
      <Text>Form removed - ready for reimplementation</Text>
    </Column>
  );
}
